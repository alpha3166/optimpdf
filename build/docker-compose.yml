version: '3'
services:
  optimpdf:
    build: .
    image: optimpdf:build
    user: ${UID}:${GID}
    volumes:
      - ${HOME}/.m2:/myhome/.m2
      - ..:/proj
    working_dir: /proj
    environment:
      - MAVEN_CONFIG=/myhome/.m2
    command: mvn -Duser.home=/myhome package
